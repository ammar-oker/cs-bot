import { styled } from '@mui/material/styles';

export const StyledModal = styled('div')`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  border-radius: 10px;
  width: 100%;
  padding: 0 0.5rem;
  max-width: 600px;
`;
